name: pull request

on: 
  pull_request:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/reusable_build.yml
  
  test:
    needs: [build]
    uses: ./.github/workflows/reusable_test.yml
